cmake_minimum_required(VERSION 2.8)
project( deteccion_conos )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable(det_conos main.cpp conos.cpp)
target_link_libraries( det_conos ${OpenCV_LIBS} )
